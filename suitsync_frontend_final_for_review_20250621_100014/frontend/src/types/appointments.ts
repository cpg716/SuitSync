export enum AppointmentStatus {
  scheduled = 'scheduled',
  rescheduled = 'rescheduled',
  canceled = 'canceled',
  completed = 'completed',
}

export enum AppointmentType {
  fitting = 'fitting',
  pickup = 'pickup',
  final_try = 'final_try',
  other = 'other',
} 