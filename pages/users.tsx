import Layout from '../components/Layout';
<Badge className="text-green-600">{...}</Badge>
<Badge>{user.role === 'admin' ? 'destructive' : 'secondary'}</Badge>
<Badge>{userData.role === 'admin' ? 'destructive' : 'secondary'}</Badge>
<Badge className="text-green-600">{...}</Badge>
<Badge>{...}</Badge>
<Badge className="text-green-600">{...}</Badge> 